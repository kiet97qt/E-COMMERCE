{
  "name": "src",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "set NODE_ENV=dev&& node --watch ./server.js",
    "start:pro": "set NODE_ENV=pro&& node --watch ./server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "compression": "^1.7.4",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "helmet": "^7.0.0",
    "mongoose": "^7.5.2"
  },
  "devDependencies": {
    "morgan": "^1.10.0"
  }
}
